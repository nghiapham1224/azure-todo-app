trigger:
  branches:
    include:
      - prod
      - dev
  paths:
    exclude:
      - terraform/**
      - pipelines/azure-infrastructure.yml

pool:
  vmImage: "ubuntu-latest"

variables:
  # --- Project Variables ---
  projectPrefix: "todo"
  environment: "dev"
  pythonVersion: "3.13"

  # --- Service Connection ---
  serviceConnection: "sp-deploy"

stages:
  - stage: BuildDeploy
    displayName: "Build & Deploy App"
    jobs:
      # =========================================================
      # JOB 1: BUILD
      # =========================================================
      - job: Build
        displayName: "Build Python App"
        steps:
          - task: UsePythonVersion@0
            displayName: "Use Python $(pythonVersion)"
            inputs:
              versionSpec: $(pythonVersion)

          - script: |
              python -m venv antenv
              source antenv/bin/activate
              pip install -r requirements.txt
            displayName: "Install Dependencies"

          # Create a suitable archive for Azure Functions
          - task: ArchiveFiles@2
            displayName: "Archive files"
            inputs:
              rootFolderOrFile: $(System.DefaultWorkingDirectory)
              includeRootFolder: false
              archiveType: "zip"
              archiveFile: "$(Build.ArtifactStagingDirectory)/$(Build.BuildId).zip"
              replaceExistingArchive: true

          - task: PublishBuildArtifacts@1
            inputs:
              PathtoPublish: $(Build.ArtifactStagingDirectory)
              ArtifactName: "drop"
              publishLocation: "Container"

      # =========================================================
      # JOB 2: DEPLOY BACKEND (FIXED FOR FLEX CONSUMPTION)
      # =========================================================
      - job: DeployBackend
        displayName: "Deploy Function App"
        dependsOn: Build
        steps:
          - task: DownloadBuildArtifacts@0
            inputs:
              buildType: "current"
              downloadType: "single"
              artifactName: "drop"
              downloadPath: $(System.ArtifactsDirectory)

          - task: AzureCLI@2
            displayName: "Deploy Backend Code"
            inputs:
              azureSubscription: "$(serviceConnection)"
              scriptType: "bash"
              scriptLocation: "inlineScript"
              inlineScript: |
                # 1. Get Resource Names
                echo "Looking for resources..."
                FUNC_NAME=$(az functionapp list -g rg-$(projectPrefix)-$(environment) --query "[?starts_with(name, 'func-$(projectPrefix)-$(environment)')].name | [0]" -o tsv)
                STORAGE_NAME=$(az storage account list -g rg-$(projectPrefix)-$(environment) --query "[?starts_with(name, 'sa$(projectPrefix)$(environment)')].name | [0]" -o tsv)

                echo "Found Function: $FUNC_NAME"
                echo "Found Storage: $STORAGE_NAME"

                # Set variable for next steps
                echo "##vso[task.setvariable variable=funcAppName]$FUNC_NAME"

                # 2. Get Storage Key & Connection String (CRITICAL FIX)
                echo "Getting Storage Keys..."
                STORAGE_KEY=$(az storage account keys list -g rg-$(projectPrefix)-$(environment) -n $STORAGE_NAME --query "[0].value" -o tsv)

                # Construct connection string manually
                CONN_STRING="DefaultEndpointsProtocol=https;AccountName=$STORAGE_NAME;AccountKey=$STORAGE_KEY;EndpointSuffix=core.windows.net"

                # 3. Upload Zip
                echo "Uploading zip to container 'deployment-package'..."
                az storage blob upload \
                  --account-name $STORAGE_NAME \
                  --account-key $STORAGE_KEY \
                  --container-name deployment-package \
                  --name "$(Build.BuildId).zip" \
                  --file "$(System.ArtifactsDirectory)/drop/$(Build.BuildId).zip" \
                  --overwrite

                # 4. Generate SAS URL
                # Flex Consumption needs a SAS token if not using Managed Identity
                END_DATE=$(date -u -d "60 minutes" '+%Y-%m-%dT%H:%MZ')
                SAS_TOKEN=$(az storage blob generate-sas \
                  --account-name $STORAGE_NAME \
                  --account-key $STORAGE_KEY \
                  --container-name deployment-package \
                  --name "$(Build.BuildId).zip" \
                  --permissions r \
                  --expiry $END_DATE \
                  -o tsv)

                # Construct the full signed URL
                BLOB_URL="https://$STORAGE_NAME.blob.core.windows.net/deployment-package/$(Build.BuildId).zip?$SAS_TOKEN"

                # 5. Apply Settings (FIXED)
                echo "Updating App Settings..."

                # Set both Connection String AND Package URL
                az functionapp config appsettings set \
                  --resource-group rg-$(projectPrefix)-$(environment) \
                  --name $FUNC_NAME \
                  --settings "DEPLOYMENT_STORAGE_CONNECTION_STRING=$CONN_STRING" "WEBSITE_RUN_FROM_PACKAGE=$BLOB_URL"

          # 6. Health Check (Retry Loop)
          - script: |
              MAX_RETRIES=20
              WAIT_TIME=10
              ATTEMPTS=0
              FUNC_URL="https://$(funcAppName).azurewebsites.net/api/health"

              echo "Waiting for app to start..."
              while [ $ATTEMPTS -lt $MAX_RETRIES ]; do
                sleep $WAIT_TIME
                RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" $FUNC_URL)
                echo "Attempt $((ATTEMPTS+1)): $RESPONSE"
                if [ "$RESPONSE" -eq "200" ]; then
                  echo "Success! App is healthy."
                  exit 0
                fi
                ATTEMPTS=$((ATTEMPTS+1))
              done
              echo "##vso[task.logissue type=warning]App failed to start within time limit."
              exit 1
            displayName: "Health Check"

      # =========================================================
      # JOB 3: DEPLOY FRONTEND
      # =========================================================
      - job: DeployFrontend
        displayName: "Deploy Frontend"
        dependsOn: DeployBackend
        steps:
          - checkout: self

          - task: AzureCLI@2
            displayName: "Configure & Get Secrets"
            inputs:
              azureSubscription: "$(serviceConnection)"
              scriptType: "bash"
              scriptLocation: "inlineScript"
              inlineScript: |
                # 1. Get Function Name
                FUNC_NAME=$(az functionapp list -g rg-$(projectPrefix)-$(environment) --query "[?starts_with(name, 'func-$(projectPrefix)-$(environment)')].name | [0]" -o tsv)
                FULL_API_URL="https://$FUNC_NAME.azurewebsites.net/api/todos"

                # 2. Inject URL
                echo "Injecting API URL: $FULL_API_URL"
                sed -i "s|__BACKEND_API_URL__|$FULL_API_URL|g" templates/index.html

                # 3. Get SWA Token Manually (Reliable Fallback)
                echo "Fetching SWA Token..."
                SWA_NAME=$(az staticwebapp list -g rg-$(projectPrefix)-$(environment) --query "[?starts_with(name, 'swa-$(projectPrefix)-$(environment)')].name | [0]" -o tsv)
                TOKEN=$(az staticwebapp secrets list -n $SWA_NAME -g rg-$(projectPrefix)-$(environment) --query "properties.apiKey" -o tsv)

                if [ -z "$TOKEN" ]; then
                  echo "##vso[task.logissue type=error]Failed to retrieve SWA Token!"
                  exit 1
                fi

                echo "##vso[task.setvariable variable=swaToken;issecret=true]$TOKEN"

          - task: AzureStaticWebApp@0
            displayName: "Deploy Static Content"
            inputs:
              app_location: "/templates"
              api_location: ""
              output_location: ""
              azure_static_web_apps_api_token: $(swaToken)
